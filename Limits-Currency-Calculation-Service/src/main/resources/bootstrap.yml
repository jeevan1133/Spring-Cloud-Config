spring:
  application:
    name: currency-calculation-service

server:
  port: 8100


eureka:
  client:
    serviceUrl:
      defaultZone: http://eurekaserver:8761/eureka

REST:
  SERVICE:
    HOSTNAME: exchangeservice
